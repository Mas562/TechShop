# TechShop - Интернет-магазин техники

Современный интернет-магазин техники на Django с адаптивным дизайном и полным функционалом электронной коммерции.

## Возможности

- 📱 Адаптивный дизайн на Bootstrap 5
- 🛒 Корзина покупок с управлением товарами
- 🔍 Поиск и фильтрация по категориям
- 👤 Регистрация и аутентификация пользователей
- 📦 Система заказов
- 🖼️ Загрузка изображений товаров
- 👨‍💼 Панель администратора Django
- 💾 База данных SQLite (легко переключается на PostgreSQL)

## Требования

- Python 3.8+
- pip
- virtualenv (рекомендуется)

## Установка и запуск

### 1. Клонирование/создание проекта

Создайте папку проекта и поместите туда все файлы.

### 2. Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Применение миграций

```bash
python manage.py migrate
```

### 5. Создание суперпользователя

```bash
python manage.py createsuperuser
```

Введите желаемое имя пользователя, email и пароль.

### 6. Загрузка тестовых данных

```bash
python manage.py load_sample_data
```

Эта команда создаст:
- 5 категорий товаров
- 20 товаров с описаниями

### 7. Запуск сервера

```bash
python manage.py runserver
```

Откройте браузер и перейдите по адресу: `http://127.0.0.1:8000/`

## Структура проекта

```
techshop/
├── manage.py
├── requirements.txt
├── .gitignore
├── README.md
├── techshop/              # Основные настройки проекта
│   ├── __init__.py
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── shop/                  # Основное приложение магазина
│   ├── migrations/
│   ├── management/
│   │   └── commands/
│   │       └── load_sample_data.py
│   ├── templates/
│   │   └── shop/
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py
│   ├── views.py
│   └── urls.py
├── users/                 # Приложение для пользователей
│   ├── templates/
│   │   └── users/
│   ├── __init__.py
│   ├── admin.py
│   ├── forms.py
│   ├── models.py
│   ├── views.py
│   └── urls.py
├── static/               # Статические файлы (CSS, JS, изображения)
└── media/                # Загруженные файлы (изображения товаров)
```

## Использование

### Панель администратора

1. Перейдите по адресу: `http://127.0.0.1:8000/admin/`
2. Войдите с учетными данными суперпользователя
3. Управляйте категориями, товарами, заказами и пользователями

### Основные URL

- Главная страница: `http://127.0.0.1:8000/`
- Каталог: `http://127.0.0.1:8000/products/`
- Корзина: `http://127.0.0.1:8000/cart/`
- Регистрация: `http://127.0.0.1:8000/users/register/`
- Вход: `http://127.0.0.1:8000/users/login/`
- Админка: `http://127.0.0.1:8000/admin/`

### Добавление товаров

1. Войдите в админку
2. Перейдите в раздел "Products"
3. Нажмите "Add Product"
4. Заполните форму и загрузите изображение
5. Сохраните

## Переход на PostgreSQL

Для использования PostgreSQL вместо SQLite:

1. Установите PostgreSQL и создайте базу данных

2. Установите драйвер:
```bash
pip install psycopg2-binary
```

3. Измените настройки в `techshop/settings.py`:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'techshop_db',
        'USER': 'your_username',
        'PASSWORD': 'your_password',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

4. Примените миграции заново:
```bash
python manage.py migrate
python manage.py createsuperuser
python manage.py load_sample_data
```

## Разработка

### Создание новых миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### Сбор статических файлов для продакшена

```bash
python manage.py collectstatic
```

## Возможные проблемы

### Ошибка с MEDIA_ROOT

Убедитесь, что папка `media/` существует и имеет права на запись.

### Ошибка импорта модулей

Убедитесь, что виртуальное окружение активировано и все зависимости установлены:
```bash
pip install -r requirements.txt
```

## Лицензия

Свободное использование для образовательных целей.

## Поддержка

Для вопросов и предложений создавайте issue в репозитории проекта.